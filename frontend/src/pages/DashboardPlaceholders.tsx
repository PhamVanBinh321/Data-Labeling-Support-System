import React from 'react';

// Reviewer Placeholder
export const ReviewerQueue: React.FC = () => (
  <div>
    <h1>Reviewer: Review Queue</h1>
    <p>Giao diện danh sách task chờ duyệt sẽ được xây dựng trong Phase 5.</p>
  </div>
);
